name: CI

on:
  - push
  - pull_request

# Add concurrency group to control job running
concurrency:
  group: ${{ github.event_name }}-${{ github.ref }}-${{ github.actor }}
  cancel-in-progress: true

permissions:
  checks: write
  contents: read
  issues: write
  pull-requests: write

jobs:
  test:
    name: Run Tests
    uses: Onemind-Services-LLC/actions/.github/workflows/netbox-plugin-tests.yml@d2fe16ad346b4649168d9becf8a1a63e79f14808
    with:
      app-id: ${{ vars.APP_ID }}
      plugin-name: 'netbox_metatype_importer'
      plugin-configuration: '{"github_token": "ghp_XXXXXXXXXXXXXXXXXXXXXX"}'
    secrets:
      private-key: ${{ secrets.APP_PRIVATE_KEY }}
